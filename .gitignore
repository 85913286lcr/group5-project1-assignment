# Raw data — too large for git, regenerate locally instead
# jobs_clean.csv is committed (not ignored) so Streamlit Community Cloud can serve the dashboard
SGJobData.csv
sgJobData.db

# WSL/Windows download metadata junk
*:Zone.Identifier

# Editor / scratch files
*.code-workspace
*.ipynb.bak
.vscode/
__pycache__/
